<?php

namespace red
{
	/**
	 * IPlugin is merely an add-on for classes that wish to declare themselves
	 * as a plugin. More specific types of plugin interfaces must extend this interface
	 * so that all plugins can be recognized as instanceof IPlugin.
	 */
	interface IPlugin
	{
		
	}
}

#EOF