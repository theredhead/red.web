<?php

namespace red
{
	/**
	 * this interface signifies that a class is testable.
	 * (i.e. that all parameterless public methods with an annotation "@test" are test methods).
	 */
	interface ITestable
	{
		// nothing is defined by this interface
	}
}
#EOF